export type Meal = {
  idMeal: string;
  strMeal: string;
  strMealThumb: string;
};

export type Product = {
  id_producto: number;
  nombre: string;
  descripcion: string;
  precio: number;
  url: string;
};

export type Category = {
  id_categoria: number;
  nombre: string;
};
